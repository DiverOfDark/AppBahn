version: v2
modules:
  - path: api
    lint:
      use:
        - STANDARD
      except:
        - PACKAGE_DIRECTORY_MATCH
        - FILE_LOWER_SNAKE_CASE
        - SERVICE_SUFFIX
        - RPC_RESPONSE_STANDARD_NAME
    breaking:
      use:
        - FILE
